<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap"
    rel="stylesheet">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap');

    * {
      font-family: 'Space Grotesk', sans-serif;
    }

    :root {
      --theme-bg-color: #1E1E1E;
      --theme-color: #CECDCE;
      --font-size: 16px;
      --line-height: 24px;
      --btn-bg: #EAEAEA;
      --btn-color: #000000;
    }

    @media only screen and (max-width: 475px) {
      .mobile-image {
        height: 415px !important;
      }
    }

    @media only screen and (max-width: 500px) {
      html {
        width: 100vw !important;
        overflow: hidden;
      }

      body {
        /* scale: 2;
        position: relative;
        top: 50vh; */
      }
    }

    html {
      box-sizing: content-box;
      width: 500px;
      background-color: var(--theme-bg-color);
      color: var(--theme-color);
      text-align: center;
      margin: 0 auto;

      font-size: var(--font-size);
      line-height: var(--line-height);
      overflow: hidden;
    }

    body {
      /* background: linear-gradient(-145deg, #49383b -5%, #2F2224 85%); */
      background: var(--theme-bg-color);
      width: auto;
      height: 100vh;
      margin: 0 auto;
    }

    h1 {
      font-weight: 400;
      margin: 1em 0;
      line-height: 1.2em;
      font-size: 2em;
      margin: 1em 0;
    }

    img {
      /* position: absolute; */
      width: 100%;
      bottom: 0;
      margin-top: 1em;
    }

    p {
      min-width: 24%;
      max-width: 100%;
      margin: 2em;
    }

    .btn {
      border-radius: 30px;
      background-color: var(--btn-bg);
      color: var(--btn-color);
      border: 1px solid rgba(206, 205, 206, 0.6);
      text-decoration: none;
      padding: 0.75rem 4em;
      margin: 0.75rem;
      font-weight: 700;
      font-size: 1.2em;
      position: relative;
      top: 1em;
    }
  </style>
</head>

<body>


  <div style="width: 100%; height: auto; margin: 0 auto; position: relative; z-index: 3; 
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;">
    <h1>
      Meet Zola. A new tool
      for self-discovery.
    </h1>
  </div>

  <p>
    Our AI understands and guides you through
    a journey of exploration to uncover your
    innermost beliefs and unlock your true potential.
  </p>

  <div style="height: max-content;">
    <a href="/verify-phone" class="btn">
      Get Started
    </a>
  </div>


  <div class="mobile-image" style="
  width: 100%;
    height: 500px;
    margin-top: 2em;
    position: relative;
    bottom: 0;
    z-index: 2;
    background: url(images/phone.png);
    background-size: contain;
    background-repeat: no-repeat;
    background-position: bottom;">
    <!-- <img src="./images/phone.png" alt="" /> -->
  </div>

</body>

</html>